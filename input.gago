-- Isso é um comentário

x = (2 + 2) * 3;
y = 20;

IFIF (x < y) {
    PRIPRINT "#{x} é menor que #{y}";
} ELSESE {
    PRIPRINT "#{x} é maior que #{y}";
}

WHIWHILE (x < 15) {
    x = x + 1;
    PRIPRINT "x = #{x}";
}

FOFOR (i, 0, 5) {
    PRIPRINT "i = #{i}";
}

FUFUNCTION media(a, b) {
    x = (a + b) / 2;
    RETUTURN x;
}

teste = media(0, 5.5);
PRIPRINT teste;