-- Isso é um comentário

x = (2 + 2) * 3;
y = 20;

IFIF (NONOT(x < y)) {
    PRIPRINT "#{x} é maior que #{y}";
} ELSESE {
    PRIPRINT "#{x} é menor que #{y}";
}

WHIWHILE (x < 15) {
    x = x + 1;
    PRIPRINT "x = #{x}";
}

meu_array = ["A", "B", "C", "D", "E"];

FOFOR (i, 0, 4) {
    PRIPRINT "letra = #{meu_array[i]}";
}

FUFUNCTION media(a, b) {
    x = (a + b) / 2;
    RETUTURN x;
}

teste = media(0, 5.5);
PRIPRINT teste;